
const events = require('events');

class Logger extends events.EventEmitter {

    newLog(message){

        //Raising an event
        this.emit('io.logging', {
            "data": message,
            "timestamp": Date.now()        
        });
    
    }
}

module.exports.Logger = Logger;